```mermaid
graph TD
    A[fa:fa-user 开发人员] -->|fabsdk.New方法+配置文件| B(创建SDK实例)
    B-->|ChannelContext方法| C(创建客户端上下文)
    C-->|将其作为参数传入channel.New方法并调用| C(创建客户端上下文)
    C-->|ChannelContext方法| D(创建一个通道客户端)
    C --> D{Select}
    C -->|One| D[Laptop]
    C -->|Two| E[iPhone]
    C -->|Three| F[Car]
  
```